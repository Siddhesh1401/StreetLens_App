{
  "name": "streetlens-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for StreetLens push notifications",
  "main": "lib/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "private": true
}
